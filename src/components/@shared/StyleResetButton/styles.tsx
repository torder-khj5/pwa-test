import styled from '@emotion/styled';

export const StyleResetButton = styled.button`
  padding: 0;
  overflow: visible;
  font: inherit;
  cursor: pointer;
  background: inherit;
  border: none;
  border-radius: 0;
  box-shadow: none;
`;
